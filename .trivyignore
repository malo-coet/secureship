# CVE-2026-48818 : SSRF via StaticFiles — l'app ne sert aucun fichier statique, code non atteint.
# À réévaluer si un StaticFiles est ajouté, ou dès que FastAPI supporte starlette >= 1.1.0.
CVE-2026-48818

# CVE-2026-54283 : DoS via request.form() — aucun endpoint form dans l'app, code non atteint.
# À réévaluer dès que FastAPI supporte starlette >= 1.3.1.
CVE-2026-54283
